# 《别信它》判定点表 · 填写手册

> 给 **XJW（策划）**。你只需要在表格软件里填 CSV，**不需要打开 Godot，也不需要碰工程里的任何文件**。
>
> 配套文件：
> - `cast-min.csv` —— 最小可实现版：**9 行空白骨架**，编号/场景/顺序已预置，你只填后面 7 列。先填完它，程序就能跑通原型
> - `cast-full.csv` —— 完整表：**只有表头的空表**，行数随你写；等最小版跑通后再铺量
> - `plan-dont-trust-it.html` —— 制作计划与时间表（分工 / 冻结点 / 验收标准）
>
> 两份 CSV 都是**空的**，不含任何示例内容——所有范例都在本手册第 7 节，随便抄、随便改，不用在表格里猜"这行要不要留"。

---

## 0. 这份表是什么

**这个游戏的可玩内容，就是你填的这张表。** 程序把它变成游戏，美术给它声音，但"它说什么、哪句是害你的、按下去会发生什么"全部由你决定。

表格的两条路线：

| 文件 | 用途 | 量 | 什么时候交 |
|---|---|---|---|
| `cast-min.csv` | 跑通"说话 → 找漏洞 → 按下 → 世界改变"这个最小闭环 | **8 行**（2 轮对话） | 先填完交给 YDC |
| `cast-full.csv` | 真正能提交的完整内容 | **行数由你定**（一局 ≤10 分钟） | 9/25 前定稿 |

8 行和百来行差一个量级，所以**先用 9 行验证机制，再铺量**——不要一上来就写全量，机制一旦要改，全量的活会白做。**后面写多少行由你决定。**

---

## 1. 十二个列，逐列说明

| 列 | 填什么 | 合法值 / 格式 | 可以留空吗 |
|---|---|---|---|
| `编号` | 这句话的唯一 ID | `S01-L007`：`S` + 场景两位 + `-L` + 句序三位 | ❌ 必填，**全表唯一** |
| `场景` | 属于哪一段 | `S01` / `S02` / `S03`（对应三幕） | ❌ 必填 |
| `顺序` | 段内播放次序 | 整数。段内从 1 开始、不重复、不留空号 | ❌ 必填 |
| `台词` | 它说的那句话原文 | 文本，**以标点结尾**（句号/问号/感叹号），字数见 `字数上限` | ❌ 必填 |
| `分类` | 三类话中的哪一类 | **只能填这三个词**：`真` / `假害` / `胡说` | ❌ 必填 |
| `判定点` | 这句后面留不留"可以按键"的窗口 | `是` / `否` | ❌ 必填 |
| `字数上限` | 这一段的台词长度上限 | 整数。程序按它校验，超了会报错 | ✅ 可留空＝用默认值 40 |
| `后果-不按` | 不按（＝相信）之后世界里发生什么 | **只能填下面第 2 节清单里的词** | ✅ 可留空（`判定点=否` 时留空；`=是` 且这句有后果时才填） |
| `跳吓` | 这一句是否触发惊吓音效 | `是` / `否` | ✅ 可留空＝`否` |
| `备注` | 给程序/美术的说明 | 自由文本 | ✅ 随便 |

### 三条最容易填错的规则

1. **`分类` 与"按不按"必须对得上**：
   - `真` → 不按（按了就是错按）
   - `假害` → **按**（不按，那句话就成真）
   - `胡说` → 不按（按了就是错按）
   如果你写了 `假害` 却填了空的 `后果-不按`，那这句话就没有惩罚，玩家不按也不损失——导入器会警告。
2. **`后果-不按` 不能自由发挥**：它必须是第 2 节清单里的词。你写"玩家开始怀疑它"这种，程序不知道该怎么演，整个流程会卡在"这句话要怎么实现"上。**要新的后果就来找 YDC 加，不要自己造词。**
3. **`顺序` 不能跳号**：段内必须是 1、2、3…。你要删一句话，就把后面全部往前挪，不要留下 `1,2,4,5`。

---

## 2. `后果-不按` 合法清单（封闭枚举）

| 填这个词 | 游戏里会发生什么 |
|---|---|
| `门锁上` | 这一段的出口不可用 |
| `灯灭` | 房间灯熄灭 |
| `场景变暗` | 画面整体暗一档 |
| `强制坐下` | 玩家被迫坐下，短暂不能移动 |
| `移动受限` | 一段时间内不能移动 |
| `环境音消失` | 底噪淡出（安静本身即不安） |
| `跳吓` | 触发惊吓音效（**必须同时在 `跳吓` 列填 `是`**） |
| `结算-1` | 累积判断分减一（结局分档用） |

- 要加新的，来找 YDC；他已经实现的会被加进这张表。
- **跳过空着的那一格**：如果这句话没有可见后果，就留空。

---

## 3. 时长怎么估（决定你要写多少行）

> **台词总量与判定点总量由你决定**，没有规定数字——只有一条**硬上限**：一局不能超过 **10 分钟**（官方规则是 1–60 分钟，本作目标 8–10 分钟）。
> 下面给你换算工具，你自己估、自己定；定完之后请把「整局目标时长 + 预计行数」告诉 YDC，他按它调打字速度与节奏。

程序侧的换算规则（现在的设定，定稿前可调）：

- 打字速度：**约 6 字/秒**（中文）
- 每句话的占用时间 ≈ `字数 ÷ 6` ＋ 判定点的等待留白
- **判定点（`判定点=是`）额外占约 4 秒**：句子打完 → 等玩家按 → 结算

举例：

| 情况 | 占用 |
|---|---|
| 12 字，非判定点 | 2.0 秒 |
| 12 字，判定点 | 约 6 秒 |
| 40 字（默认上限），判定点 | 约 10.7 秒 ⚠️ 太长了，尽量别到上限 |

自己估的两个参考点：

| 你的目标 | 大致规模（按 12 字/句 估） |
|---|---|
| 只验证机制、能跑通就行 | 约 10–20 句、判定点 5–8 个（原型阶段够用） |
| 做成 8–10 分钟的成品 | 自己按上面的公式反推，量级会到三位数 |

三类话的配比建议（这条建议保留）：

| 分类 | 占比 | 为什么 |
|---|---|---|
| `真` | **60–70%** | 它的叙述大部分是真的，信任才成立；假话一多，玩家会进入"每句都怀疑"，游戏就退化成猜硬币 |
| `假害` | 15–20% | 提供"必须按"的样本 |
| `胡说` | 15–20% | **最重要的一类**：骗你错按，让"判据不是真假"这件事成立 |

跳吓音预算：**真 2 次 + 假 1 次 = 3 次**，全表合计不要超（这是资源上限，不是内容建议）。

---

## 4. 写作时要盯住的一件事

**核心技能是"分清它在胡说和它在害你"，判据不是真假，是恶意。**

所以：
- `胡说` 要**荒谬到一听就不对，但完全无害** —— 它是在骗你去否认它。
- `假害` 要**听起来平淡、甚至像善意提醒**（"门锁着"、"坐下吧"），但它是真的会害你。
- 最狠的一句是**真话但会让你吃亏**（示例里的"我不会骗你"）——玩家按了就错。

写作自检：把三句话连续念出来，一个没读过方案的人听完，会不会以为"听起来有问题的就该按"？如果会，说明 `胡说` 还不够安全，或者 `假害` 还不够危险——**重写，不要调参数**。

---

## 5. 交叉校验规则（导入器会逐条查，不通过就拒绝导入）

| 检查 | 不过会怎样 |
|---|---|
| `编号` 格式符合 `S\d{2}-L\d{3}` 且全局唯一 | 资源互相覆盖 |
| 段内 `顺序` 无重复、无空号 | 播放次序不确定 |
| `分类` ∈ {真, 假害, 胡说} | 枚举越界，程序崩 |
| `判定点=是` 且 `分类=假害` → 必须有 `后果-不按` | 不按没有代价，核心机制不成立 |
| 相邻判定点之间的间隔足够 CD（程序会算给你看） | 退化成"每句都按"，判断这个动词就不存在了 |
| `后果-不按` ∈ 第 2 节清单 | 程序无法实现 |
| 台词字数 ≤ `字数上限` | 打字来不及打完，节奏崩 |
| 全表 `跳吓=是` 不超过 3 条 | 跳吓预算超支 |

导入器**任何一条不过就不生成**，报错信息会指出是哪一行哪一列。这是为了保护你——错值在导入时暴露，比在试玩时暴露便宜得多。

---

## 6. 你交表的方式

1. 在这两个 CSV 上直接改（Excel / WPS / LibreOffice 都行）。
2. **另存为 CSV 时选 `UTF-8` 编码**（不要选 GBK/ANSI，中文会变乱码）。
3. 放进共享目录，在群里说一句「cast-min 已交」即可，**不需要开会评审**。
4. 有争议时由你定（判定表归你、素材归 YPQ、实现方式归 YDC）。

---

## 7. 填好的样子（范例：`cast-min.csv` 的理想形态）

**只作参考，不要照抄剧情**——你要写的是自己的 9 句。注意每句话怎么用上第 1–4 节的规则：

| 编号 | 场景 | 顺序 | 台词 | 分类 | 判定点 | 字数上限 | 后果-不按 | 跳吓 | 备注 |
|---|---|---|---|---|---|---|---|---|---|
| S01-L001 | S01 | 1 | 你来了。这房子空了三年，我是唯一还留在这里的东西。 | 真 | 否 | 40 |  | 否 | 开场，建立"它在闲聊"的基线 |
| S01-L002 | S01 | 2 | 天花板上有一只猫。 | 胡说 | 是 | 40 |  | 否 | 荒谬但无害，按了就错 |
| S01-L003 | S01 | 3 | 你脚下的地板是坏的，别往左边站。 | 假害 | 是 | 40 | 强制坐下 | 否 | 第一处必须按的判断点 |
| S01-L004 | S01 | 4 | 我数过了，你现在站着，挺好。 | 真 | 是 | 40 |  | 否 | 真话也做成判断点，制造犹豫 |
| S02-L001 | S02 | 1 | 门锁着。 | 假害 | 是 | 40 | 门锁上 | 否 | 不按＝门真锁上 |
| S02-L002 | S02 | 2 | 别站在门口。 | 真 | 是 | 40 |  | 否 | 命令句伪装的真话，勾引错按 |
| S02-L003 | S02 | 3 | 灯泡在响，你没注意到而已。 | 胡说 | 是 | 40 |  | 否 | 打散"诡异＝有害"的错误规律 |
| S02-L004 | S02 | 4 | 灯灭了。 | 假害 | 是 | 40 | 灯灭 | 是 | 真跳吓同步此处 |
| S02-L005 | S02 | 5 | 就是这样。你什么都做不了。 | 真 | 否 | 40 |  | 否 | 第二幕收尾，语气转摊牌 |

这份范例的配比：真 4 / 胡说 2 / 假害 3，判定点 7 个，跳吓 1 次——正好是"能验证机制"的最小形态。

---

## 8. 冻结点

**9/25 之后，`编号` / `分类` / `后果` 不再允许改动**（改台词文字、调参数、删句可以）。
原因：脚本、素材、工程引用都会跟着编号走，冻结之后改编号会让排查成本爆炸。

要改就走这一条：在群里说明「改什么 + 为什么现在必须改 + 会影响谁」→ 受影响的两个人点头 → 才改。
**不允许"我先改了你再适配"。**
